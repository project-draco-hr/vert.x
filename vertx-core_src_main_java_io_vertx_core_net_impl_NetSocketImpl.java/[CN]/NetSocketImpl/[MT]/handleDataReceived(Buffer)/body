{
  checkContext();
  if (paused) {
    if (pendingData == null) {
      pendingData=new ArrayDeque<>();
    }
    pendingData.add(data);
    return;
  }
  if (dataHandler != null) {
    dataHandler.handle(data);
  }
}
