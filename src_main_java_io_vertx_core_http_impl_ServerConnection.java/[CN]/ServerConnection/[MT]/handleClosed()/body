{
  if (ws != null) {
    metrics.disconnected(ws.metric);
    ws.metric=null;
  }
  super.handleClosed();
  if (ws != null) {
    ws.handleClosed();
  }
  if (pendingResponse != null) {
    pendingResponse.handleClosed();
  }
}
