{
  vertx.executeBlocking(new Action<Void>(){
    public Void perform(){
      ChoosableSet<V> vals=map.get(k);
      if (vals != null) {
        vals.remove(v);
        if (vals.isEmpty()) {
          map.remove(k);
        }
      }
      return null;
    }
  }
,completionHandler);
}
