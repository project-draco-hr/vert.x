{
  if (connected) {
    socket.write(message.writeToWire());
  }
 else {
synchronized (this) {
      if (connected) {
        socket.write(message.writeToWire());
      }
 else {
        pending.add(message);
      }
    }
  }
}
