{
synchronized (this) {
    available.clear();
    waiters.clear();
  }
  for (  ClientConnection conn : allConnections) {
    try {
      conn.close();
    }
 catch (    Throwable t) {
      log.error("Failed to close connection",t);
    }
  }
  allConnections.clear();
}
