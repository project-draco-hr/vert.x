{
synchronized (VertxImpl.this) {
    if (--refCount == 0) {
      releaseWorkerExecutor(name);
      if (metrics != null) {
        metrics.close();
      }
      workerExec.shutdownNow();
    }
  }
}
