{
synchronized (conn) {
    if (unknownFrameHandler != null) {
      try {
        unknownFrameHandler.handle(frame);
      }
 catch (      Throwable t) {
        handleException(t);
      }
    }
  }
}
