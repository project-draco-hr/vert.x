{
  Buffer b=randomBuffer(100);
  buff.setBuffer(50,b);
  byte[] b2=buff.getBytes(50,150);
  assertTrue(buffersEqual(b,new Buffer(b2)));
  byte[] b3=randomByteArray(100);
  buff.setBytes(50,b3);
  byte[] b4=buff.getBytes(50,150);
  assertTrue(buffersEqual(new Buffer(b3),new Buffer(b4)));
}
