{
  if (pipelining) {
    if (conn.getOutstandingRequestCount() == 0 && waiters.isEmpty()) {
      conn.close();
    }
  }
 else   if (keepAlive) {
    checkReuseConnection(conn);
  }
 else {
    conn.close();
  }
}
