{
  LOGGER.info("Stopping redeployment");
  closed=true;
  if (service != null) {
    try {
      service.close();
    }
 catch (    IOException e) {
      LOGGER.error("Cannot stop the watcher service",e);
    }
  }
  undeploy.handle(null);
}
