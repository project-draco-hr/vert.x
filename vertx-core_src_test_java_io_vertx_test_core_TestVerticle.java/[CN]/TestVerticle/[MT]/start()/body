{
  processArgs=vertx.currentContext().processArgs();
  vertx.eventBus().send("testcounts",new JsonObject().putString("deploymentID",vertx.currentContext().deploymentID()).putNumber("count",instanceCount.incrementAndGet()));
}
